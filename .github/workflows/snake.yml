name: Generate Snake

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  generate:
    permissions:
      contents: write

    runs-on: ubuntu-latest

    steps:
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: Socc171

          outputs: |
                  dist/github-contribution-grid-snake.svg?palette=github-dark&color_snake=#ff004c,#ff7a00,#ffe600,#00ff85,#00c2ff,#a855f7
                  dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#ff004c,#ff7a00,#ffe600,#00ff85,#00c2ff,#a855f7

      - name: push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
